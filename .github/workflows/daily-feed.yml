name: Daily Aignal Feed

on:
  push:
    branches:
      - main
  schedule:
    # 06:00 America/Los_Angeles during daylight saving time.
    - cron: "0 13 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  generate:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Generate daily feed
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          PUBLIC_BASE_URL: ${{ vars.PUBLIC_BASE_URL }}
        run: npm run generate-feed -- --fresh-window-hours=36

      - name: Validate daily feed
        run: npm run validate-feed -- --min-source-images=6 --require-images

      - name: Commit generated feed archive
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          mkdir -p public/images
          git add public/feed public/images
          git diff --cached --quiet || git commit -m "Update daily Aignal feed"
          git push

      - name: Install web dependencies
        run: npm ci --prefix web

      - name: Build web app
        env:
          NEXT_PUBLIC_BASE_PATH: /${{ github.event.repository.name }}
        run: npm run build-web

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/out

      - id: deployment
        uses: actions/deploy-pages@v4
